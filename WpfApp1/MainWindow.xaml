<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1" xmlns:view_model="clr-namespace:WpfApp1.NVVM.View_Model"
        mc:Ignorable="d"
        WindowStyle="None"
        Height="650" Width="1200"
        Background="#36393F"
        ResizeMode="CanResizeWithGrip">

    <Window.DataContext>
        <view_model:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

<!--top panel border-->
        <Border Grid.ColumnSpan="2"
                Background="#252525"
                MouseDown="Border_MouseDown">

            <Grid HorizontalAlignment="Stretch">
                <Label Content="Final Project For now"
                       Foreground="Gray"
                       FontWeight="SemiBold"/>

<!--butuns for top panel -->
                <StackPanel HorizontalAlignment="Right"
                            Orientation ="Horizontal">

                    <Button Width="20" Height="20"
                            Content="_"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="Gray"
                            FontWeight="Bold"
                            Margin="0,0,0,3"
                            Click="btn_Minimize_Click"/>

                    <Button Width="20" Height="20"
                            Content="▢"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="Gray"
                            FontWeight="Bold"
                            Click="btn_Maximize_Click"/>  

                    <Button Width="20" Height="20"
                            Content="✕"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="Red"
                            FontWeight="Bold"
                            Click="btn_Close_Click"/>

                </StackPanel>
                
            </Grid>

        </Border>


        <Grid Background="#2F3136"
              Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <Label Content="Contacts"
                   Foreground="Gray"
                   FontWeight="Medium"
                   VerticalAlignment="Center"
                   Margin="8, 0, 0, 0"/>

            <ListView ItemsSource="{Binding Contacts}"
                      Background="Transparent"
                      BorderBrush="0"
                      Grid.Row="1"
                      ItemContainerStyle="{StaticResource Contact_Card}"/>


        </Grid>
        

    </Grid>
    
</Window>
